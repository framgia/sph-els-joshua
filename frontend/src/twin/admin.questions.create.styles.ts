import tw from 'twin.macro'

export const styles = {
  main: tw`flex justify-center w-full py-8`,
  section: [
    tw`overflow-x-auto relative rounded-2xl shadow-md bg-white`,
    tw`max-w-lg mx-auto border w-full`
  ]
}
